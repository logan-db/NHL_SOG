# The main pipeline for nhlPredict
resources:
  pipelines:
    nhlPredict_pipeline:
      name: nhlPredict_pipeline
      target: nhlPredict_${bundle.environment}
      libraries:
        - notebook:
            path: /Repos/logan.rupert@databricks.com/NHL_SOG/ingestion/01-Ingestion-DLT-v2.py

      configuration:
        bundle.sourcePath: /Workspace/${workspace.file_path}/src
